# =========================================
# CONFIGURACIoN GENERAL DE LA APLICACIoN
# =========================================
spring.application.name=products-service
server.port=${SERVER_PORT:8080}
server.servlet.context-path=/api

# =========================================
# CONFIGURACIoN DE BASE DE DATOS - MYSQL
# =========================================
# URL de conexion
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME}

# Pool de conexiones HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# =========================================
# CONFIGURACIoN JPA/HIBERNATE
# =========================================
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQL8Dialect}
spring.jpa.show-sql=false

# =========================================
# CONFIGURACIoN DE LOGGING
# =========================================
logging.level.root=${LOGGING_LEVEL:INFO}
logging.level.com.linktic.products=${LOGGING_LEVEL:INFO}
logging.level.org.hibernate.SQL=WARN

# Configuracion de archivos de log
logging.file.name=logs/products-service.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=30
logging.logback.rollingpolicy.total-size-cap=1GB

# Patron de logging
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n

# =========================================
# CONFIGURACIoN DE ACTUATOR (MONITORING)
# =========================================
management.endpoints.web.exposure.include=health,info,metrics,prometheus,loggers
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=always
management.info.env.enabled=true
management.info.java.enabled=true
management.info.os.enabled=true

# Configuracion personalizada de health checks
management.health.db.enabled=true
management.health.diskspace.enabled=true
management.health.ping.enabled=true

# Metricas
management.metrics.enable.jvm=true
management.metrics.enable.process=true
management.metrics.enable.system=true
management.metrics.enable.tomcat=true
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# =========================================
# CONFIGURACIoN DE JACKSON (JSON)
# =========================================
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Bogota
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.serialization.indent-output=true

# =========================================
# CONFIGURACIoN DE VALIDACIoN
# =========================================
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# =========================================
# CONFIGURACIoN DE CACHE
# =========================================
spring.cache.type=simple
spring.cache.cache-names=products
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=30m

# =========================================
# CONFIGURACIoN DE TRANSACCIONES
# =========================================
spring.transaction.default-timeout=30
spring.transaction.rollback-on-commit-failure=true

# =========================================
# CONFIGURACIoN ESPECiFICA DEL DOMINIO
# =========================================
# Configuracion de paginacion
app.pagination.default-page-size=5
app.pagination.max-page-size=100

# Configuracion de validaciones de negocio
app.products.name-max-length=255
app.products.description-max-length=1000
app.products.price-min-value=50.00
app.products.price-max-value=999999999.99

# =========================================
# CONFIGURACIoN DE COMUNICACIoN ENTRE MICROSERVICIOS
# =========================================
# URL del microservicio de inventory
#app.services.inventory.url=http://inventory-service:8081/api/v1
#app.services.inventory.timeout.connection=2000
#app.services.inventory.timeout.read=4000

# =========================================
# CONFIGURACIoN DE RETRY Y CIRCUIT BREAKER
# =========================================

#resilience4j.retry.instances.inventory-service.max-attempts=3
#resilience4j.retry.instances.inventory-service.wait-duration=2s
#resilience4j.retry.instances.inventory-service.exponential-backoff-multiplier=2

#resilience4j.circuitbreaker.instances.inventory-service.failure-rate-threshold=50
#resilience4j.circuitbreaker.instances.inventory-service.slow-call-rate-threshold=50
#resilience4j.circuitbreaker.instances.inventory-service.slow-call-duration-threshold=2s
#resilience4j.circuitbreaker.instances.inventory-service.minimum-number-of-calls=5
#resilience4j.circuitbreaker.instances.inventory-service.sliding-window-size=10
#resilience4j.circuitbreaker.instances.inventory-service.wait-duration-in-open-state=30s

# =========================================
# INFORMACIoN DE LA APLICACIoN
# =========================================
info.app.name=Product_Service
info.app.description=Microservicio de gestion de productos

# =========================================
# CONFIGURACIoN DE DESARROLLO/DEBUG
# =========================================
# Solo para desarrollo - NUNCA en produccion
spring.h2.console.enabled=false
spring.jpa.properties.hibernate.generate_statistics=false

# =========================================
# CONFIGURACIoN DE SEGURIDAD BaSICA
# =========================================
# Configuracion CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:8080
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true
app.cors.max-age=3600
